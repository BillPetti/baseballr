# **Column structure of MLB Stats Live Game API data frame**

An empty tibble

## Usage

``` r
stats_api_live_empty_df
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 0
rows and 131 columns.
