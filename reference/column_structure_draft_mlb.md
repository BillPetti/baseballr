# **Column structure of the MLB Draft data**

A tibble giving column structure of MLB Draft data

## Usage

``` r
column_structure_draft_mlb
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 0
rows and 72 columns.
