% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.ncaa_is_interstitial}
\alias{.ncaa_is_interstitial}
\title{\strong{Detect a stats.ncaa.org Akamai interstitial challenge}}
\usage{
.ncaa_is_interstitial(body)
}
\arguments{
\item{body}{A character scalar: the response body (HTML) as text.}
}
\value{
\code{TRUE} when \code{body} looks like an Akamai interstitial, else \code{FALSE}.
}
\description{
Some \code{stats.ncaa.org} routes (e.g. \verb{/teams/\{id\}/season_to_date_stats}) are
gated behind Akamai Bot Manager. When challenged, the server returns HTTP 200
but the body is a short interstitial shell (a \code{bm-verify} meta-refresh, an
\code{akamai_validation.html} iframe, or a \code{request_quota_reached.html} notice)
rather than the data page. A static request cannot solve the challenge, so we
detect the shell and degrade gracefully instead of silently scraping a page
that has no data tables.
}
\keyword{internal}
