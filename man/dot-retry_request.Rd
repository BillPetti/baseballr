% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_espn.R
\name{.retry_request}
\alias{.retry_request}
\title{Perform an HTTP GET request with retry logic (ESPN)}
\usage{
.retry_request(
  url,
  params = list(),
  headers = NULL,
  timeout = 60,
  proxy = NULL
)
}
\arguments{
\item{url}{The URL to request.}

\item{params}{Named list of query parameters (default: empty list).}

\item{headers}{Named character vector of headers (default: NULL).}

\item{timeout}{Timeout in seconds (default: 60).}

\item{proxy}{Optional proxy (see above). Defaults to
\code{getOption("baseballr.proxy")}.}
}
\value{
An \link[httr2:response]{httr2::response} object.
}
\description{
Thin \code{httr2} wrapper used by the ESPN MLB wrappers. Supports optional query
parameters, custom headers, and proxy routing. Proxy resolution order:
\enumerate{
\item \code{proxy} argument (caller-supplied, highest precedence).
\item \code{getOption("baseballr.proxy")} (session-level fallback -- set once with
\code{options(baseballr.proxy = ...)}; ESPN wrappers call \code{.retry_request()}
directly without \code{...}, so per-call overrides are not threaded through).
\item \code{http_proxy} / \code{https_proxy} / \code{no_proxy} env vars (read by libcurl
automatically when no explicit proxy is supplied).
}
}
\details{
The \code{proxy} value accepts a single URL string (\code{"http://host:port"}, passed
to \code{httr2::req_proxy(url = )}) or a named list spread as keyword args into
\code{httr2::req_proxy()} (\code{url}, \code{port}, \code{username}, \code{password}, \code{auth}).
}
\keyword{internal}
