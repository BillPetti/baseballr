% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fg_projections.R
\name{fg_projections}
\alias{fg_projections}
\title{\strong{Scrape FanGraphs player projections}}
\usage{
fg_projections(
  type = "steamer",
  stats = "bat",
  team = 0,
  league = "all",
  position = "all"
)
}
\arguments{
\item{type}{Projection system. One of \code{"steamer"}, \code{"zips"}, \code{"zipsdc"},
\code{"atc"}, \code{"thebat"}, \code{"thebatx"} (batters only), or the rest-of-season
variants \code{"rsteamer"}, \code{"rzips"}, \code{"ratc"}, \code{"rthebat"}, \code{"rthebatx"}.
Defaults to \code{"steamer"}.}

\item{stats}{Either \code{"bat"} (batting projections) or \code{"pit"} (pitching
projections). Defaults to \code{"bat"}.}

\item{team}{MLB team filter as FanGraphs' numeric team id; \code{0} (default)
returns all teams.}

\item{league}{League filter: \code{"all"} (default), \code{"al"}, or \code{"nl"}.}

\item{position}{Position filter (e.g. \code{"all"}, \code{"c"}, \code{"ss"}, \code{"of"}).
Defaults to \code{"all"}.}
}
\value{
A \code{baseballr_data} tibble with one row per player and the
projection system's stat columns (counting stats, rates, and projected
WAR; column set varies by system and by \code{stats}).
}
\description{
Retrieves FanGraphs' published player projections (Steamer,
ZiPS, ATC, THE BAT, and their variants) from the FanGraphs projections
API -- one row per player with the projected stat line.
}
\examples{
\donttest{
  try(fg_projections(type = "steamer", stats = "bat"))
}
}
