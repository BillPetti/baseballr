% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{bref_read_html}
\alias{bref_read_html}
\title{\strong{Read HTML from Baseball Reference with retry on rate-limit (429)}}
\usage{
bref_read_html(url, max_tries = 3L)
}
\arguments{
\item{url}{A Baseball Reference URL.}

\item{max_tries}{Integer. Maximum number of attempts (default 3).}
}
\value{
An XML document as returned by \code{\link[xml2:read_html]{xml2::read_html()}}.
}
\description{
Baseball Reference rate-limits (HTTP 429) scrapers that make rapid
sequential requests.  \code{xml2::read_html()} has no built-in retry, so a
429 surfaces as a generic error and the caller's \code{tryCatch} swallows it
into a misleading "Invalid arguments or no data available" message (#344).

This helper uses \code{httr2::req_retry()} to retry 429 / 503 responses
with exponential backoff (up to 3 tries), then passes the response
body to \code{xml2::read_html()}.  A 5-second courtesy delay mirrors the
existing NCAA scraper convention.
}
\keyword{internal}
