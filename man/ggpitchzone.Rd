% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ggpitchzone.R
\name{ggpitchzone}
\alias{ggpitchzone}
\title{\strong{Plot pitch locations with the strike zone with ggplot2}}
\usage{
ggpitchzone(
  data,
  x_value = "plate_x",
  y_value = "plate_z",
  color_value = "pitch_type",
  sz_top = "sz_top",
  sz_bot = "sz_bot",
  point_alpha = 0.6,
  point_size = 2
)
}
\arguments{
\item{data}{A data frame of pitch data containing plate-crossing
coordinates and strike-zone bounds.}

\item{x_value}{Column with the horizontal plate-crossing coordinate in
feet (catcher's perspective). Defaults to \code{"plate_x"}.}

\item{y_value}{Column with the vertical plate-crossing coordinate in feet.
Defaults to \code{"plate_z"}.}

\item{color_value}{Categorical column to color points by, as a string.
Defaults to \code{"pitch_type"}, colored with the Savant palette; pass another
column (or \code{NULL} for uncolored points) to override.}

\item{sz_top}{Column with the batter's strike-zone top (feet). Defaults to
\code{"sz_top"}; the zone rectangle uses the column means.}

\item{sz_bot}{Column with the batter's strike-zone bottom (feet). Defaults
to \code{"sz_bot"}.}

\item{point_alpha}{Alpha for the pitch points. Defaults to \code{0.6}.}

\item{point_size}{Size of the pitch points. Defaults to \code{2}.}
}
\value{
A ggplot2 object: pitch locations from the catcher's perspective
with the strike zone drawn as a rectangle.
}
\description{
Plots pitch locations from the catcher's perspective with the batter's
strike zone overlaid, from Statcast pitch data as returned by
\code{\link[=statcast_search]{statcast_search()}} or \code{\link[=mlb_pbp]{mlb_pbp()}} (the latter via its
\verb{pitchData.coordinates.*} columns renamed to \code{plate_x} / \code{plate_z}).
Colors default to Baseball Savant's pitch-type palette via
\code{\link[=statcast_pitch_colors]{statcast_pitch_colors()}}.
}
\examples{
\donttest{
  try({
    pitches <- statcast_search("2024-06-01", "2024-06-01")
    ggpitchzone(pitches)
  })
}
}
